services:
  postgres:
    image: postgres:17
    container_name: waywarden-postgres
    environment:
      POSTGRES_DB: waywarden
      POSTGRES_USER: waywarden
      POSTGRES_PASSWORD: waywarden
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
